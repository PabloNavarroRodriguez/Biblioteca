import { PipeGeneroPipe } from './pipe-genero.pipe';

describe('PipeGeneroPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeGeneroPipe();
    expect(pipe).toBeTruthy();
  });
});
