import { PipeFiltroPipe } from './pipe-filtro.pipe';

describe('PipeFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
